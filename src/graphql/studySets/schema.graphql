# import * from './type.graphql'

type Query {
    studySet: StudySet
    studySets(criteria: StudySetInput, limit: Int, skip: Int, sort: StudySetSortCases): [StudySet]
}
type Mutation {
    createStudySet: StudySet
    updateStudySet: StudySet
    deleteStudySet: StudySet
}